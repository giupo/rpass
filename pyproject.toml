[project]
name = "rpass"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MasterOfPuppets", email = "giuseppe.acito@bancaditalia.it" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
rpass = "rpass:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "bump2version>=1.0.1",
    "gitchangelog>=3.0.4",
]
