% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manage.r
\name{pass_rm}
\alias{pass_rm}
\title{Remove a password store entry (or directory of entries)}
\usage{
pass_rm(name, store = pass_store(), recursive = FALSE, force = FALSE)
}
\arguments{
\item{name}{Entry name, or a directory prefix when \code{recursive = TRUE}.}

\item{store}{Store root, see \code{\link[=pass_store]{pass_store()}}.}

\item{recursive}{If \code{TRUE}, \code{name} is treated as a directory and all
entries under it are removed.}

\item{force}{If \code{FALSE} (default), errors when there is nothing to remove.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Remove a password store entry (or directory of entries)
}
\examples{
store <- tempfile("rpass-store-")
keyring <- tempfile("rpass-keyring-")
Sys.setenv(PASSWORD_STORE_DIR = store, RPASS_KEYRING_DIR = keyring)

keys <- rpass:::generate_test_keypair("Demo <demo@example.com>", "hunter2")
public_path <- tempfile(fileext = ".asc")
writeLines(keys$public, public_path)
fp <- pass_import_pubkey(public_path)
pass_init("", fp)
pass_insert("todelete", "pw")

pass_rm("todelete")
pass_rm("nothing-here", force = TRUE) # would otherwise error

Sys.unsetenv(c("PASSWORD_STORE_DIR", "RPASS_KEYRING_DIR"))
}
