% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert.r
\name{pass_insert}
\alias{pass_insert}
\title{Insert a new password store entry}
\usage{
pass_insert(
  name,
  password,
  fields = list(),
  notes = character(),
  store = pass_store(),
  keyring = pass_keyring_dir(),
  force = FALSE
)
}
\arguments{
\item{name}{Entry name relative to \code{store} (e.g. \code{"Lavoro/SECRET"}).}

\item{password}{Password/secret (first line of the entry). May be a plain
string or an \link[=new_rpass_secret]{rpass_secret}.}

\item{fields}{Named list of extra \code{key: value} fields.}

\item{notes}{Additional freeform lines (character vector).}

\item{store}{Store root, see \code{\link[=pass_store]{pass_store()}}.}

\item{keyring}{Local public-key keyring, see \code{\link[=pass_keyring_dir]{pass_keyring_dir()}}.}

\item{force}{If \code{FALSE} (default), refuses to overwrite an existing entry.}
}
\value{
The written \link[=new_rpass_secret]{rpass_secret}, invisibly.
}
\description{
Encrypts \code{password} (plus optional \code{fields}/\code{notes}) to the recipients
configured for this location (the nearest ancestor \code{.gpg-id}, see
\code{\link[=pass_init]{pass_init()}}), whose public keys must already be present in the local
rpass keyring (see \code{\link[=pass_import_pubkey]{pass_import_pubkey()}}).
}
\details{
Unlike \verb{pass insert -m}'s interactive multi-line prompt, this takes
structured arguments -- more scriptable, and avoids terminal-raw-mode
handling in a library context.
}
