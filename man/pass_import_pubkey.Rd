% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keyring.R
\name{pass_import_pubkey}
\alias{pass_import_pubkey}
\title{Import a public key into the local rpass keyring}
\usage{
pass_import_pubkey(armor_file, keyring = pass_keyring_dir())
}
\arguments{
\item{armor_file}{Path to an armored (\code{--armor}) exported OpenPGP public
key, e.g. from \verb{gpg --export --armor <id> > recipient.asc}.}

\item{keyring}{Local keyring directory, see \code{\link[=pass_keyring_dir]{pass_keyring_dir()}}.}
}
\value{
The recipient's fingerprint, invisibly.
}
\description{
\code{pass}/GnuPG resolve a \code{.gpg-id} recipient identifier against GnuPG's local
keyring (\code{pubring.kbx}), which rpass intentionally does not parse. Instead,
rpass keeps its own small directory of armored public keys (see
\code{\link[=pass_keyring_dir]{pass_keyring_dir()}}), populated explicitly via this function, once per
recipient.
}
