% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.r
\name{pass_key}
\alias{pass_key}
\title{Load and unlock a GPG secret key}
\usage{
pass_key(
  path,
  passphrase = getPass::getPass("GPG passphrase: "),
  set_default = TRUE
)
}
\arguments{
\item{path}{Path to an armored (\code{--armor}) exported OpenPGP secret key.}

\item{passphrase}{Passphrase for the key. Defaults to a hidden prompt via
\code{\link[getPass:getPass]{getPass::getPass()}} -- avoid passing it as a literal argument in a
script, since that can leak into shell/R history.}

\item{set_default}{If \code{TRUE} (default), sets this as the session's default
key so \verb{key=} does not need to be supplied to other \verb{pass_*()} calls.}
}
\value{
An \code{rpass_key} object.
}
\description{
Reads an OpenPGP secret key exported with
\code{gpg --export-secret-keys --armor > mykey.asc} and unlocks it with a
passphrase. The passphrase is validated immediately (a wrong passphrase
fails here, not on the first \code{\link[=pass_show]{pass_show()}}). rpass never reads GnuPG's
live agent-managed keyring (\verb{~/.gnupg/private-keys-v1.d}) -- only an
explicitly exported armored key file.
}
