% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.r
\name{pass_generate}
\alias{pass_generate}
\title{Generate a random password store entry}
\usage{
pass_generate(
  name,
  length = 25,
  symbols = TRUE,
  in_place = FALSE,
  store = pass_store(),
  key = NULL,
  keyring = pass_keyring_dir(),
  force = FALSE
)
}
\arguments{
\item{name}{Entry name relative to \code{store}.}

\item{length}{Password length.}

\item{symbols}{If \code{TRUE} (default), include punctuation symbols in the
character set; otherwise letters and digits only.}

\item{in_place}{If \code{TRUE}, decrypt the existing entry first (via \code{key})
and preserve its \code{fields}/\code{notes} verbatim, replacing only the password
line -- mirrors \verb{pass generate -i}. If \code{FALSE} (default), creates a new
entry with no extra fields/notes.}

\item{store}{Store root, see \code{\link[=pass_store]{pass_store()}}.}

\item{key}{An \code{rpass_key}, only needed when \code{in_place = TRUE}. Defaults to
the session's default key.}

\item{keyring}{Local public-key keyring, see \code{\link[=pass_keyring_dir]{pass_keyring_dir()}}.}

\item{force}{If \code{FALSE} (default), refuses to overwrite an existing entry
(ignored when \code{in_place = TRUE}, which always overwrites).}
}
\value{
The generated \link[=new_rpass_secret]{rpass_secret}, invisibly.
}
\description{
Generate a random password store entry
}
\examples{
store <- tempfile("rpass-store-")
keyring <- tempfile("rpass-keyring-")
Sys.setenv(PASSWORD_STORE_DIR = store, RPASS_KEYRING_DIR = keyring)

keys <- rpass:::generate_test_keypair("Demo <demo@example.com>", "hunter2")
secret_path <- tempfile(fileext = ".asc")
public_path <- tempfile(fileext = ".asc")
writeLines(keys$secret, secret_path)
writeLines(keys$public, public_path)

key <- pass_key(secret_path, passphrase = "hunter2")
fp <- pass_import_pubkey(public_path)
pass_init("", fp)

pass_generate("Lavoro/NEW", length = 24)
pass_generate("Lavoro/NEW", in_place = TRUE) # rotate, keeping fields/notes

Sys.unsetenv(c("PASSWORD_STORE_DIR", "RPASS_KEYRING_DIR"))
}
