% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keyring.R
\name{lookup_pubkey}
\alias{lookup_pubkey}
\title{Look up an armored public key by \code{.gpg-id} recipient identifier}
\usage{
lookup_pubkey(id, keyring = pass_keyring_dir())
}
\arguments{
\item{id}{A \code{.gpg-id} recipient identifier (fingerprint or key id).}

\item{keyring}{Local keyring directory, see \code{\link[=pass_keyring_dir]{pass_keyring_dir()}}.}
}
\value{
Armored public key text.
}
\description{
Tries an exact filename match first (\verb{<id>.asc}); on a miss, scans every
\code{.asc} file in the keyring, computes its real fingerprint, and matches by
suffix (so a short key id or a full fingerprint both work), rebuilding the
cached index as it goes.
}
\keyword{internal}
